dependencies {
    
    compile 'org.apache.logging.log4j:log4j-1.2-api:' + versionProps['log4j.version']
    compile 'commons-io:commons-io:' + versionProps['commons-io.version']
    
    testCompile 'junit:junit:' + versionProps['junit.version']
}
